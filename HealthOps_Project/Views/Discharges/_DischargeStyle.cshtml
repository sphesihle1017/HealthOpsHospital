<style>
    .beds-title-bar {
        background: linear-gradient(90deg, #0d6d3d, #28a745, #0d6d3d);
        font-size: 1.8rem;
        letter-spacing: 1px;
        box-shadow: 0 4px 8px rgba(0,0,0,0.2);
    }

    .table {
        border-radius: 12px;
        overflow: hidden;
    }

    .table-success {
        background: linear-gradient(90deg, #6ccf95, #8be6ac);
        color: #034d1f;
        font-size: 1.05rem;
    }

    .bed-row {
        background: linear-gradient(90deg, #c8f7dc, #eafaf1, #c8f7dc);
    }
    .bed-row:nth-child(even) {
        background: linear-gradient(90deg, #b6f2d0, #e0f7ea, #b6f2d0);
    }

    .btn-success { background: #6ccf95; border: none; }
    .btn-success:hover { background: #57b87d; transform: scale(1.05); }

    .btn-warning { background: #ffe082; border: none; }
    .btn-warning:hover { background: #ffd54f; transform: scale(1.05); }

    .btn-danger { background: #f28b82; border: none; color: white; }
    .btn-danger:hover { background: #e57373; transform: scale(1.05); }

    .btn-info { background: #64b5f6; border: none; }
    .btn-info:hover { background: #42a5f5; transform: scale(1.05); }
</style>
